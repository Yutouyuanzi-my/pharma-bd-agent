# 2026-07-13 工作日志

## git 安全清理（关键）
- 用户要求清理 git 并删除 `.env.example`。
- 发现 `.env.example` 曾硬编码真实 DeepSeek API Key（已被 git 追踪、进入历史），且该 key 片段被误写入 `.workbuddy/memory/MEMORY.md`。
- 处理：
  1. 改 `run.sh`：移除对 `.env.example` 的依赖，改为首次运行时用内置 heredoc 模板自动生成 `.env`（含 DEEPSEEK_API_KEY/BASE_URL/MODEL 占位）。
  2. 改 `README.md` Quick Start，去掉"复制 .env.example"说明。
  3. `git rm -f .env.example` 并删除工作区文件。
  4. `git filter-branch --index-filter 'git rm --cached --ignore-unmatch .env.example'` 重写全部 27 个提交，清除历史文件；`git reflog expire --expire=now --all` + `git gc --prune=now` 彻底回收旧对象。
  5. 编辑 MEMORY.md 脱敏并 `git commit --amend` 重写 HEAD，消除密钥残留。
- 验证：扫描全部 112 个 git 对象，`sk-80c1` 与 `sk-` 前缀均 0 处；`.env.example` 已从所有历史提交移除；无远程仓库。
- 仍建议用户去 DeepSeek 控制台重置该 key 以彻底作废（仅本地、无公网泄露）。
- 新增"安全约定"：禁止在任何文件/记忆/提交中写入真实密钥或片段。

## 结论
项目 git 历史现已干净，可安全推送到 GitHub。

## 部署准备（Streamlit Cloud 方向）
- 用户目标：先把真实 app 部署到 Streamlit Cloud，作为求职硬证据（比 codebuddy.work 的静态原型更有说服力）。
- 前置确认：`.gitignore` 已排除 `.env`/`.venv`（真实 key 不泄露）；requirements 依赖齐全；git 历史干净。
- 修复部署 blocker：原 `app.py` 用 `os.getenv` 在导入时读 key，Streamlit Cloud 走 `st.secrets`，会读不到。已加 `_get_cfg()` 助手（st.secrets 优先、env 兜底），并修正侧边栏 key 校验（line 644）。已提交 `62c1037`。
- 待办（需用户本人）：`gh` CLI 未安装且需 GitHub 账号授权才能 push；Streamlit Cloud 部署需在控制台 Secrets 配置同名键值（DEEPSEEK_API_KEY/BASE_URL/MODEL）。
- 注意：Streamlit Cloud 免费版要求仓库 public（对求职作品反而更好）。
